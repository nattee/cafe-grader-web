- if conf.value_type.downcase == 'boolean'
  .form-check.form-switch
    %a{href: toggle_grader_configuration_path(conf), 'data-turbo': "true", 'data-turbo-method': :patch}
      %input.form-check-input{id: dom_id(conf,"config"),type: :checkbox, checked: conf.value.downcase == 'true'}
- else
  = turbo_frame_tag dom_id(conf,"config"), data: {turbo: "true"} do
    = conf.value
    = link_to '<span class="mi mi-bs md-18">edit</span>'.html_safe, edit_grader_configuration_path(conf), data: {turbo: "true"}
