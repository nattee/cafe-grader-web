= turbo_frame_tag :problem_table_frame do
  %table#problem_table.table.datatable.table-hover
    %thead
      %tr
        %th Name
        %th Full name
        %th Enabled
        %th Remove
    %tbody
      -# we use datatable ajax data source (see _groups_users.haml for example)
