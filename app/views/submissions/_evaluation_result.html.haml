-# render the code with explanation
.row
  .col-12
    %span.grader-comment.me-2= Evaluation.result_enum_to_code(ev.result)
    %span.text-secondary
      %em (#{t(ev.result, scope: "activerecord.enums.order.status")})
-# show error badge
-# TODO: FIX THIS!!!! won't work when ev is a testcase
- if ['grader_error','unknown_error'].include? ev.result
  .badge.text-bg-danger Please notify the admin
-# render comment if any
- unless ev.result_text.blank? || ev.result_text == Evaluation.result_enum_to_code(ev.result)
  %div.text-body-tertiary
    = ev.result_text
