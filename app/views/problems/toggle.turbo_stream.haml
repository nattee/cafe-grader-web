= turbo_stream.append 'js-response' do
  :javascript
    set_problem_switch(#{@problem.id},#{@problem.available},#{@problem.view_testcase})
- if @toast
  = turbo_stream.append 'toast-area', partial: 'shared/toast', locals: {toast: @toast}
