= turbo_stream.update :hint_select, partial: 'hint_select'
-# = turbo_stream.update :hint_detail, partial: 'datasets/form'
- if @toast
  = turbo_stream.append 'toast-area', partial: 'toast', locals: {toast: @toast}
