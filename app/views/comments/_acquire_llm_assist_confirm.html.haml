-# `cost` is what THIS requester would be charged (Llm::CommentAssist.assist_cost_for):
-# 0 for an admin asking on a student's behalf, or when the site price is 0.
-# Control flow cannot live inside the :markdown filter, hence the helper.
:markdown
  This action will send the problem statement, your __original__ source code 
  from submission ##{@submission.id}, and its score 
  (#{@submission.points} [#{@submission.grader_comment}]) to the
  `#{model}` large language model with a predefined prompt to request assistance.

  #{llm_assist_price_sentence(cost)}

  The generated assistance will remain available to you permanently.

  Do you want to confirm and request assistance?
