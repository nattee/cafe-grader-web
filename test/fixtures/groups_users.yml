john_in_group_a:
  group: group_a
  user: john
  role: 0
  enabled: true

admin_editor_group_a:
  group: group_a
  user: admin
  role: 2
  enabled: true

mary_editor_group_a:
  group: group_a
  user: mary
  role: 2
  enabled: true

james_in_group_a:
  group: group_a
  user: james
  role: 0
  enabled: true

# reba is a plain reporter of group_a (role 1) — used to test that reporters
# stay gated by Problem#available / Group#enabled while editors (mary) bypass.
# (Kept off `jack`, which other suites rely on as the "user in no group" actor.)
reba_reporter_group_a:
  group: group_a
  user: reba
  role: 1
  enabled: true
