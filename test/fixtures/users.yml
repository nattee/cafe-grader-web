<%
User.public_class_method :encrypt

salt = "abc"
%>

john:
  login: john
  full_name: john
  hashed_password: <%= User.encrypt("hello",salt) %>
  salt: <%= salt %>
  activated: true

admin:
  login: admin
  full_name: admin
  hashed_password: <%= User.encrypt("admin",salt) %>
  salt: <%= salt %>
  roles: admin
  activated: true

mary:
  login: mary
  full_name: Mary Editor
  hashed_password: <%= User.encrypt("mary",salt) %>
  salt: <%= salt %>
  activated: true

james:
  login: james
  full_name: James
  hashed_password: <%= User.encrypt("morning",salt) %>
  salt: <%= salt %>
  activated: true

jack:
  login: jack
  full_name: Jack
  hashed_password: <%= User.encrypt("morning",salt) %>
  salt: <%= salt %>
  activated: true

# A plain reporter of group_a (see groups_users.yml). Dedicated to the report
# scope tests so `jack` stays the canonical "user in no group" actor.
reba:
  login: reba
  full_name: Reba Reporter
  hashed_password: <%= User.encrypt("reba",salt) %>
  salt: <%= salt %>
  activated: true

disabled_user:
  login: disabled
  full_name: Disabled User
  hashed_password: <%= User.encrypt("disabled",salt) %>
  salt: <%= salt %>
  activated: false
